# Seedbed 产品设计文档

> *"Where ideas take root."*

Seedbed 是 AI Agent 工作流的「苗床」—— 通过对话将模糊需求转化为结构化的 Linear Issues，为后续的无人值守编程奠定基础。

---

## 一、产品定位

### 1.1 核心问题

现有 AI 编程工具（如 Claude Code Harness）的痛点：

| 痛点 | 表现 |
|------|------|
| 计划质量依赖 Session 1 | 如果 feature_list.json 拆得不好，后续全跑偏 |
| 无法人工介入调整 | 生成后只能全盘接受或重来 |
| 缺乏可视化 | 看不到任务全貌，无法评估工作量 |
| 与项目管理脱节 | 计划存在本地文件，团队无法协作 |

### 1.2 Seedbed 的解决方案

```
开发者需求（模糊）
      │
      ▼
┌─────────────────────────────────────────┐
│              SEEDBED                     │
│                                          │
│  1. 对话式需求澄清                        │
│  2. 自动拆解 + 生成任务                   │
│  3. 可视化 Review & 调整                  │
│  4. 一键发布到 Linear                     │
│                                          │
└─────────────────────────────────────────┘
      │
      ▼
Linear Issues（结构化、可追踪、可执行）
      │
      ▼
下游系统（Ralph Wiggum / Coding Agent）
```

### 1.3 用户画像

- **主要用户**：使用 AI Agent 进行编程的开发者
- **使用场景**：启动新项目或新功能前的计划阶段
- **核心诉求**：快速、准确地将想法转化为可执行的任务列表

---

## 二、核心功能

### 2.1 功能架构

```
Seedbed
├── 🔗 项目连接 (Project Connection)
│   ├── 连接 Git 仓库（读取结构、README、现有代码）
│   ├── 连接 Linear 工作区
│   └── 导入现有 app_spec.txt / PRD
│
├── 💬 对话计划 (Conversation Planning)
│   ├── 需求澄清对话
│   ├── 自动追问缺失信息
│   └── 实时生成任务预览
│
├── 📋 计划看板 (Plan Board)
│   ├── 任务卡片展示
│   ├── 拖拽排序
│   ├── 编辑/删除/合并任务
│   ├── 添加验收标准（测试用例）
│   └── 工作量估算
│
├── 🔍 Review 面板 (Review Panel)
│   ├── 任务依赖关系图
│   ├── 覆盖度检查（需求 vs 任务）
│   └── 风险提示
│
└── 📤 发布 (Publish)
    ├── 预览 Linear Issues
    ├── 选择目标 Project / Team
    └── 一键创建所有 Issues
```

### 2.2 功能详述

#### 2.2.1 项目连接

**输入方式**：
1. Git URL（public repo 直接读取，private 需授权）
2. 本地路径（桌面版）
3. 上传 ZIP
4. 纯文本描述（无现有代码的新项目）

**自动提取的上下文**：
```json
{
  "project_name": "my-blog",
  "tech_stack": ["Next.js", "TypeScript", "Prisma", "PostgreSQL"],
  "existing_features": ["用户注册", "文章列表"],
  "directory_structure": "src/pages, src/components, prisma/schema.prisma...",
  "conventions": {
    "naming": "camelCase",
    "test_framework": "vitest",
    "package_manager": "pnpm"
  }
}
```

#### 2.2.2 对话计划

**对话流程**：

```
用户: 我想加一个评论功能

Seedbed: 好的，关于评论功能，我需要确认几点：

1. 评论层级：支持单层评论还是嵌套回复？
2. 用户权限：游客可以评论吗，还是必须登录？
3. 审核机制：评论是否需要审核后才能显示？
4. 通知：评论后是否通知文章作者？

用户: 支持嵌套回复，必须登录，不需要审核，要通知作者

Seedbed: 明白了。我已生成 12 个任务，点击「查看计划」预览。

[查看计划] [继续对话]
```

**智能追问规则**：
- 检测需求中的二义性
- 识别缺失的边界条件
- 提示潜在的技术选型决策

#### 2.2.3 计划看板

**任务卡片结构**：

```
┌─────────────────────────────────────────────────────┐
│ [P0] 评论数据模型设计                      🏷️ 后端   │
├─────────────────────────────────────────────────────┤
│ 创建 Comment 模型，包含：                           │
│ - id, content, authorId, postId                    │
│ - parentId (支持嵌套)                               │
│ - createdAt, updatedAt                             │
│                                                     │
│ 📎 涉及文件: prisma/schema.prisma                   │
│                                                     │
│ ✅ 验收标准:                                        │
│    □ prisma migrate 成功                           │
│    □ Comment 表已创建                              │
│    □ 支持自关联查询嵌套评论                         │
│                                                     │
│ ⏱️ 预估: 0.5h                          [编辑] [删除] │
└─────────────────────────────────────────────────────┘
```

**看板操作**：
- 拖拽调整优先级
- 合并相似任务
- 拆分过大任务
- 批量打标签
- 设置依赖关系

#### 2.2.4 Review 面板

**依赖关系图**：
```
                    ┌──────────────┐
                    │ 数据模型设计  │
                    └──────┬───────┘
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
        ┌──────────┐ ┌──────────┐ ┌──────────┐
        │ API 接口 │ │ 前端组件 │ │ 通知服务 │
        └────┬─────┘ └────┬─────┘ └────┬─────┘
             │            │            │
             └────────────┼────────────┘
                          ▼
                    ┌──────────┐
                    │ 集成测试 │
                    └──────────┘
```

**检查项**：
- ⚠️ 任务 #7 没有验收标准
- ⚠️ 任务 #3 和 #5 可能有重复
- ✅ 所有需求点已覆盖
- ✅ 依赖关系无循环

#### 2.2.5 发布到 Linear

**发布配置**：
```
目标工作区: My Team
目标项目: [新建项目: "Blog - 评论功能"]
标签映射:
  - 后端 → Backend
  - 前端 → Frontend
  - 测试 → QA
优先级映射:
  - P0 → Urgent
  - P1 → High
  - P2 → Medium
  - P3 → Low
```

**发布内容**：
- 创建 Linear Project
- 批量创建 Issues（带描述、标签、优先级）
- 创建 META Issue（包含完整计划摘要）
- 设置 Issue 依赖关系

---

## 三、技术架构

### 3.1 系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         SEEDBED                                  │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐         │
│  │   Frontend  │    │   Backend   │    │  AI Service │         │
│  │   (Web UI)  │◄──►│    (API)    │◄──►│  (Claude)   │         │
│  └─────────────┘    └──────┬──────┘    └─────────────┘         │
│                            │                                    │
│                            ▼                                    │
│                    ┌───────────────┐                           │
│                    │   Database    │                           │
│                    │  (PostgreSQL) │                           │
│                    └───────────────┘                           │
│                                                                  │
├──────────────────────────────────────────────────────────────────┤
│                       External Services                          │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐         │
│  │   Linear    │    │   GitHub    │    │   Claude    │         │
│  │     API     │    │     API     │    │     API     │         │
│  └─────────────┘    └─────────────┘    └─────────────┘         │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

### 3.2 技术选型建议

| 层级 | 推荐技术 | 理由 |
|------|---------|------|
| Frontend | Next.js 14 + TypeScript | SSR、App Router、生态成熟 |
| UI 组件 | shadcn/ui + Tailwind | 美观、可定制、复制即用 |
| 拖拽 | @dnd-kit/core | 现代、轻量、React 友好 |
| Backend | Next.js API Routes 或 FastAPI | 前者全栈统一，后者 Python 生态 |
| Database | PostgreSQL + Prisma | 类型安全、Linear 也用 PG |
| AI | Claude API (claude-sonnet-4-20250514) | 性价比高、擅长结构化输出 |
| 认证 | NextAuth.js | 支持 GitHub OAuth、简单 |
| 部署 | Vercel / Railway | 快速、自动化 |

### 3.3 核心数据模型

```prisma
// prisma/schema.prisma

model User {
  id            String    @id @default(cuid())
  email         String    @unique
  name          String?
  linearToken   String?   // 加密存储
  projects      Project[]
  createdAt     DateTime  @default(now())
}

model Project {
  id              String    @id @default(cuid())
  name            String
  description     String?
  userId          String
  user            User      @relation(fields: [userId], references: [id])
  
  // Git 连接
  gitUrl          String?
  gitBranch       String?   @default("main")
  
  // 提取的上下文
  techStack       String[]
  conventions     Json?
  
  // 计划
  plans           Plan[]
  
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
}

model Plan {
  id              String    @id @default(cuid())
  projectId       String
  project         Project   @relation(fields: [projectId], references: [id])
  
  name            String
  description     String?
  status          PlanStatus @default(DRAFT)
  
  // 对话历史
  conversations   Conversation[]
  
  // 任务
  tasks           Task[]
  
  // Linear 同步
  linearProjectId String?
  publishedAt     DateTime?
  
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
}

enum PlanStatus {
  DRAFT
  REVIEWING
  PUBLISHED
  ARCHIVED
}

model Task {
  id              String    @id @default(cuid())
  planId          String
  plan            Plan      @relation(fields: [planId], references: [id])
  
  title           String
  description     String
  priority        Int       @default(2)  // 0=P0, 1=P1, 2=P2, 3=P3
  labels          String[]
  
  // 验收标准
  acceptanceCriteria String[]
  
  // 关联文件
  relatedFiles    String[]
  
  // 预估
  estimateHours   Float?
  
  // 排序
  sortOrder       Int       @default(0)
  
  // 依赖
  dependsOnId     String?
  dependsOn       Task?     @relation("TaskDependency", fields: [dependsOnId], references: [id])
  dependents      Task[]    @relation("TaskDependency")
  
  // Linear 同步
  linearIssueId   String?
  
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
}

model Conversation {
  id              String    @id @default(cuid())
  planId          String
  plan            Plan      @relation(fields: [planId], references: [id])
  
  role            String    // "user" | "assistant"
  content         String
  
  createdAt       DateTime  @default(now())
}
```

---

## 四、核心 API 设计

### 4.1 对话 API

```typescript
// POST /api/plans/:planId/chat
// 发送消息并获取 AI 回复

interface ChatRequest {
  message: string;
  generateTasks?: boolean;  // 是否同时生成/更新任务
}

interface ChatResponse {
  reply: string;
  tasks?: Task[];           // 如果 generateTasks=true
  questions?: string[];     // AI 的追问
}
```

### 4.2 任务生成 API

```typescript
// POST /api/plans/:planId/generate-tasks
// 基于对话历史生成完整任务列表

interface GenerateTasksRequest {
  regenerate?: boolean;     // 是否重新生成（覆盖现有）
}

interface GenerateTasksResponse {
  tasks: Task[];
  coverage: {
    requirements: string[];
    covered: boolean[];
  };
  warnings: string[];
}
```

### 4.3 发布 API

```typescript
// POST /api/plans/:planId/publish
// 发布到 Linear

interface PublishRequest {
  linearTeamId: string;
  projectName?: string;     // 新建项目名（可选）
  existingProjectId?: string;
  labelMapping?: Record<string, string>;
  priorityMapping?: Record<number, string>;
}

interface PublishResponse {
  linearProjectId: string;
  linearProjectUrl: string;
  issues: {
    taskId: string;
    linearIssueId: string;
    linearIssueUrl: string;
  }[];
}
```

---

## 五、AI Prompt 设计

### 5.1 需求澄清 Prompt

```markdown
# System Prompt

你是 Seedbed 的计划助手，帮助开发者将模糊的需求转化为清晰的任务列表。

## 项目上下文
{project_context}

## 你的任务

1. 理解用户的需求
2. 识别需求中的模糊点和缺失信息
3. 通过追问澄清细节
4. 当信息足够时，生成结构化的任务列表

## 追问原则

- 每次最多问 3-4 个问题
- 问题要具体，给出选项
- 优先问影响架构的决策
- 不要问可以合理假设的细节

## 输出格式

如果需要追问：
```json
{
  "type": "clarification",
  "message": "你的回复",
  "questions": ["问题1", "问题2"]
}
```

如果信息足够，可以生成任务：
```json
{
  "type": "ready",
  "message": "我已理解需求，点击「生成计划」查看任务列表。",
  "summary": "需求摘要..."
}
```
```

### 5.2 任务生成 Prompt

```markdown
# System Prompt

你是一个资深的技术 Lead，擅长将需求拆解为可执行的开发任务。

## 项目上下文
{project_context}

## 需求摘要
{requirement_summary}

## 对话历史
{conversation_history}

## 任务拆解原则

1. **原子性**：每个任务应该是独立可完成的
2. **可验证**：每个任务必须有明确的验收标准
3. **合理粒度**：单个任务 0.5-4 小时，超过则拆分
4. **依赖清晰**：标注任务间的依赖关系
5. **标签分类**：后端/前端/测试/文档/基础设施

## 验收标准写法

- 使用可自动化验证的描述
- 优先写成测试用例的形式
- 例如："POST /api/comments 返回 201 且数据库有新记录"

## 输出格式

```json
{
  "tasks": [
    {
      "title": "任务标题",
      "description": "详细描述...",
      "priority": 0,  // 0=P0 最高
      "labels": ["后端", "数据库"],
      "acceptanceCriteria": [
        "验收标准1",
        "验收标准2"
      ],
      "relatedFiles": ["prisma/schema.prisma"],
      "estimateHours": 1.5,
      "dependsOnIndex": null  // 或依赖的任务索引
    }
  ],
  "warnings": [
    "任务X可能需要额外的Y支持"
  ]
}
```
```

---

## 六、UI/UX 设计要点

### 6.1 页面结构

```
┌─────────────────────────────────────────────────────────────────┐
│  🌱 Seedbed                    [Projects ▾]  [Settings]  [User] │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────┐  ┌─────────────────────────────────┐ │
│  │                      │  │                                  │ │
│  │    对话面板           │  │         计划看板                 │ │
│  │    (左侧 40%)        │  │         (右侧 60%)               │ │
│  │                      │  │                                  │ │
│  │  ┌────────────────┐  │  │  [全部] [后端] [前端] [测试]     │ │
│  │  │ AI: 关于评论... │  │  │                                  │ │
│  │  └────────────────┘  │  │  ┌─────────┐ ┌─────────┐        │ │
│  │                      │  │  │ Task 1  │ │ Task 2  │        │ │
│  │  ┌────────────────┐  │  │  │ P0 后端 │ │ P1 后端 │        │ │
│  │  │ You: 支持嵌套  │  │  │  └─────────┘ └─────────┘        │ │
│  │  └────────────────┘  │  │                                  │ │
│  │                      │  │  ┌─────────┐ ┌─────────┐        │ │
│  │  ┌────────────────┐  │  │  │ Task 3  │ │ Task 4  │        │ │
│  │  │ AI: 好的...    │  │  │  │ P1 前端 │ │ P2 测试 │        │ │
│  │  └────────────────┘  │  │  └─────────┘ └─────────┘        │ │
│  │                      │  │                                  │ │
│  │  [输入框...] [发送]  │  │                                  │ │
│  │                      │  │  ──────────────────────────────  │ │
│  │  [生成计划]          │  │  共 12 个任务 | 预估 18h          │ │
│  │                      │  │  [Review] [发布到 Linear]        │ │
│  └──────────────────────┘  └─────────────────────────────────┘ │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

### 6.2 关键交互

**对话生成任务**：
- 用户发消息 → 右侧实时出现/更新任务卡片
- 卡片有「新增」动画，标识是本轮新生成的
- 悬浮显示「点击编辑」

**任务编辑**：
- 点击卡片 → 展开详情面板（右侧滑入）
- 支持 Markdown 编辑描述
- 验收标准可逐条添加/删除
- 拖拽调整依赖关系

**发布流程**：
```
[发布到 Linear]
│
▼
┌─────────────────────────────────┐
│  选择 Linear 工作区              │
│  [▾ My Team              ]      │
│                                  │
│  项目名称                        │
│  [Blog - 评论功能        ]       │
│                                  │
│  □ 创建 META Issue (推荐)        │
│  □ 设置 Issue 依赖关系           │
│                                  │
│  预览: 将创建 12 个 Issues       │
│                                  │
│  [取消]              [确认发布]  │
└─────────────────────────────────┘
```

---

## 七、Linear 集成细节

### 7.1 OAuth 授权流程

```
1. 用户点击「连接 Linear」
2. 跳转 Linear OAuth：
   https://linear.app/oauth/authorize?
   client_id=xxx&
   redirect_uri=xxx&
   scope=read,write,issues:create&
   response_type=code

3. 用户授权后回调，获取 access_token
4. 存储 token（加密）到用户记录
```

### 7.2 Issue 创建映射

```typescript
// Seedbed Task → Linear Issue

interface LinearIssueCreate {
  title: task.title,
  description: `
${task.description}

## 验收标准
${task.acceptanceCriteria.map(c => `- [ ] ${c}`).join('\n')}

## 相关文件
${task.relatedFiles.map(f => `- \`${f}\``).join('\n')}

---
*由 Seedbed 自动创建*
  `,
  priority: priorityMapping[task.priority],
  labelIds: task.labels.map(l => labelMapping[l]),
  estimate: task.estimateHours,
  projectId: targetProjectId,
}
```

### 7.3 META Issue 模板

```markdown
# 📋 计划摘要: {planName}

## 需求概述
{requirementSummary}

## 任务统计
- 总任务数: {totalTasks}
- P0 (紧急): {p0Count}
- P1 (高): {p1Count}
- P2 (中): {p2Count}
- P3 (低): {p3Count}
- 总预估工时: {totalHours}h

## 任务列表
{tasks.map(t => `- [ ] #${t.linearIssueId} ${t.title}`).join('\n')}

## 技术栈
{techStack.join(', ')}

## 约定
{conventions}

---
*由 Seedbed 生成于 {timestamp}*
*计划 ID: {planId}*
```

---

## 八、MVP 范围建议

### Phase 1: 核心流程 (2-3 周)

- [ ] 用户认证（GitHub OAuth）
- [ ] 项目创建（手动输入描述）
- [ ] 对话界面（基础聊天）
- [ ] AI 生成任务（调用 Claude API）
- [ ] 任务列表展示（只读）
- [ ] 导出 JSON / Markdown

### Phase 2: 编辑能力 (1-2 周)

- [ ] 任务卡片编辑
- [ ] 拖拽排序
- [ ] 验收标准编辑
- [ ] 依赖关系设置

### Phase 3: Linear 集成 (1-2 周)

- [ ] Linear OAuth
- [ ] 发布到 Linear
- [ ] Issue 状态同步（可选）

### Phase 4: 增强功能 (持续)

- [ ] Git 仓库连接
- [ ] 项目上下文提取
- [ ] 多人协作
- [ ] 历史版本对比
- [ ] 模板库

---

## 九、文件结构建议

```
seedbed/
├── app/                          # Next.js App Router
│   ├── (auth)/
│   │   ├── login/page.tsx
│   │   └── callback/page.tsx
│   ├── (dashboard)/
│   │   ├── projects/page.tsx
│   │   └── projects/[id]/page.tsx
│   ├── api/
│   │   ├── auth/[...nextauth]/route.ts
│   │   ├── projects/route.ts
│   │   ├── plans/[id]/chat/route.ts
│   │   ├── plans/[id]/tasks/route.ts
│   │   └── plans/[id]/publish/route.ts
│   ├── layout.tsx
│   └── page.tsx
├── components/
│   ├── chat/
│   │   ├── ChatPanel.tsx
│   │   ├── MessageBubble.tsx
│   │   └── ChatInput.tsx
│   ├── board/
│   │   ├── PlanBoard.tsx
│   │   ├── TaskCard.tsx
│   │   ├── TaskDetail.tsx
│   │   └── DependencyGraph.tsx
│   ├── publish/
│   │   ├── PublishDialog.tsx
│   │   └── LinearPreview.tsx
│   └── ui/                       # shadcn components
├── lib/
│   ├── ai/
│   │   ├── prompts.ts
│   │   └── generate-tasks.ts
│   ├── linear/
│   │   ├── client.ts
│   │   └── publish.ts
│   ├── prisma.ts
│   └── utils.ts
├── prisma/
│   └── schema.prisma
├── public/
├── .env.example
├── package.json
└── README.md
```

---

## 十、环境变量

```bash
# .env.example

# Database
DATABASE_URL="postgresql://..."

# Auth
NEXTAUTH_SECRET="..."
NEXTAUTH_URL="http://localhost:3000"
GITHUB_CLIENT_ID="..."
GITHUB_CLIENT_SECRET="..."

# Linear
LINEAR_CLIENT_ID="..."
LINEAR_CLIENT_SECRET="..."
LINEAR_REDIRECT_URI="http://localhost:3000/api/auth/linear/callback"

# Claude
ANTHROPIC_API_KEY="sk-ant-..."
ANTHROPIC_MODEL="claude-sonnet-4-20250514"
```

---

## 十一、下游系统对接

### 11.1 输出格式兼容

Seedbed 发布到 Linear 后，下游系统（如 Ralph Wiggum / Linear Harness）可以：

1. **通过 Linear API 获取任务**：
   ```typescript
   const issues = await linearClient.issues({
     filter: {
       project: { id: { eq: projectId } },
       state: { type: { eq: "unstarted" } }
     }
   });
   ```

2. **按优先级排序执行**

3. **更新 Issue 状态**：Todo → In Progress → Done

4. **在 Issue 评论中记录进度**

### 11.2 feature_list.json 兼容

如果需要兼容原始 Harness 格式，可在发布时额外生成：

```json
{
  "features": [
    {
      "id": "1",
      "title": "评论数据模型设计",
      "test_cases": [
        "prisma migrate 成功",
        "Comment 表已创建"
      ],
      "status": "pending",
      "linear_issue_id": "ABC-123"
    }
  ]
}
```

---

*文档版本: v1.0*
*最后更新: 2025-01-05*